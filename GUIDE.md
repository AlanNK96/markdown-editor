# Markdown 编辑器 - 使用指南

## 📋 目录
1. [快速开始](#快速开始)
2. [界面介绍](#界面介绍)
3. [基本操作](#基本操作)
4. [快捷键参考](#快捷键参考)
5. [高级功能](#高级功能)
6. [安全特性](#安全特性)
7. [常见问题](#常见问题)
8. [自定义与配置](#自定义与配置)

---

## 🚀 快速开始

### 方法一：本地使用
1. 在文件管理器中找到 `index.html` 文件
2. 双击文件在浏览器中打开
3. 开始使用！

### 方法二：服务器部署
1. 将整个 `markdown-editor` 文件夹上传到 Web 服务器
2. 访问 `http://your-domain/markdown-editor/index.html`
3. 开始使用！

---

## 🖥️ 界面介绍

### 工具栏（顶部）
工具栏分为三个区域：

#### 文件操作区
- 📄 **新建**：创建新文档（快捷键：Ctrl+N）
- 📂 **打开**：打开本地 .md 或 .txt 文件（快捷键：Ctrl+O）
- 💾 **保存**：保存当前文档到本地（快捷键：Ctrl+S）

#### Markdown 工具区
提供常用 Markdown 语法的快捷按钮：
- **H**：标题
- **B**：粗体（快捷键：Ctrl+B）
- **I**：斜体（快捷键：Ctrl+I）
- **S**：删除线
- 🔗：链接（快捷键：Ctrl+K）
- 🖼️：图片
- **</>**：行内代码
- **{ }**：代码块
- **• 列表**：无序列表
- **1. 列表**：有序列表
- **❝**：引用
- **—**：水平线
- **⊞**：表格

#### 视图控制区
- **⊞**：分屏模式（左侧编辑，右侧预览）
- **✎**：仅编辑模式
- **👁**：仅预览模式
- **🌙/☀️**：深色/浅色主题切换

### 编辑区（左侧）
- 在此输入 Markdown 文本
- 按 Tab 键插入 4 个空格缩进
- 支持智能续行（列表、引用、Checkbox 按 Enter 自动续行）
- 支持所有标准快捷键（复制、粘贴等）

### 预览区（右侧）
- 实时显示 Markdown 渲染效果
- 自动更新（延迟 300ms 防抖）
- 支持 GFM（GitHub 风格 Markdown）语法
- 支持代码语法高亮（自动跟随主题切换亮色/暗色样式）
- 内容未变化时跳过渲染，优化性能

### 状态栏（底部）
显示当前状态信息：
- 文件名
- 修改状态（● 表示未保存）
- 字符数和行数统计

---

## 📝 基本操作

### 1. 新建文档
**操作步骤**：
1. 点击工具栏"新建"按钮
2. 或按下 `Ctrl+N`
3. 如果当前有未保存内容，会提示确认

**注意事项**：
- 未保存的更改会丢失（除非保存为草稿）
- 建议先保存当前文档

### 2. 打开文档
**操作步骤**：
1. 点击工具栏"打开"按钮
2. 或按下 `Ctrl+O`
3. 在文件选择对话框中选择 .md 或 .txt 文件
4. 点击"打开"

**支持的文件类型**：
- `.md`（Markdown 文件）
- `.txt`（纯文本文件）
- `.markdown`（Markdown 文件）

**文件大小限制**：
- 最大支持 10MB
- 建议单个文件不超过 5000 行（性能考虑）

### 3. 保存文档
**操作步骤**：
1. 点击工具栏"保存"按钮
2. 或按下 `Ctrl+S`
3. 浏览器会自动下载 .md 文件

**保存位置**：
- 文件会保存到浏览器的默认下载文件夹
- 文件名默认为 `untitled.md`（如果是新文档）
- 如果是打开的文件，会保持原文件名

### 4. 使用 Markdown 语法

#### 方法一：手动输入
直接在编辑区输入 Markdown 语法：
```markdown
# 标题
**粗体** *斜体*
[链接](URL)
- [x] 已完成的任务
- [ ] 未完成的任务
```

#### 方法二：使用工具栏
1. 选中要格式化的文本
2. 点击对应的工具栏按钮
3. 语法会自动添加到选中文本周围

#### 方法三：使用快捷键
- `Ctrl+B`：粗体
- `Ctrl+I`：斜体
- `Ctrl+K`：链接

### 5. 切换视图模式

#### 分屏模式（默认）
- 左侧编辑，右侧预览
- 适合边写边看

#### 仅编辑模式
- 只显示编辑区
- 适合专注写作
- 屏幕空间更大

#### 仅预览模式
- 只显示预览区
- 适合查看最终效果
- 演示文档时使用

---

## ⌨️ 快捷键参考

### 全局快捷键

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| `Ctrl/Cmd + S` | 保存文件 | 下载 .md 文件到本地 |
| `Ctrl/Cmd + O` | 打开文件 | 选择本地 .md/.txt/.markdown 文件 |
| `Ctrl/Cmd + N` | 新建文件 | 创建空白文档 |
| `Ctrl/Cmd + B` | 粗体 | 包裹选中文本为 `**粗体**` |
| `Ctrl/Cmd + I` | 斜体 | 包裹选中文本为 `*斜体*` |
| `Ctrl/Cmd + K` | 插入链接 | 插入 `[链接文本](URL)` |

### 编辑器快捷键

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| `Tab` | 缩进 | 插入 4 个空格 |
| `Enter` | 智能续行 | 在列表/引用/Checkbox 行自动续行 |
| `Enter`（空标记行） | 清除标记 | 在空列表项/空引用行自动删除标记符 |

---

## 🔥 高级功能

### 自动保存草稿

**工作原理**：
- 编辑内容 2 秒后自动保存到浏览器本地存储
- 下次打开页面时会提示是否恢复
- 草稿保存期限：7 天

**恢复草稿**：
1. 打开编辑器
2. 如果有草稿，会显示确认对话框
3. 点击"确定"恢复，点击"取消"放弃

**清除草稿**：
- 点击"取消"放弃草稿
- 或保存文件后草稿自动清除

### 代码语法高亮

**支持的语言**：
- JavaScript
- Python
- Java
- HTML
- CSS
- JSON
- 更多语言可通过配置添加

**使用方法**：
在代码块中指定语言：
````markdown
```javascript
console.log("Hello");
```
````

### 表格编辑

**插入表格**：
1. 点击表格按钮
2. 会插入 3 列 2 行表格模板（含表头）
3. 修改表头和内容

**表格语法**：
```markdown
| 列1 | 列2 | 列3 |
| --- | --- | --- |
| 内容1 | 内容2 | 内容3 |
```

**对齐方式**：
```markdown
| 左对齐 | 居中 | 右对齐 |
| :--- | :---: | ---: |
```

### 图片插入

**方法一：本地图片**
1. 将图片上传到图床（如 imgur.com）
2. 复制图片 URL
3. 点击图片按钮或使用语法：
   ```markdown
   ![图片描述](图片URL)
   ```

**方法二：相对路径**
```markdown
![图片](./images/pic.jpg)
```

### 深色/浅色主题切换

**切换方式**：
- 点击工具栏右侧的 🌙/☀️ 图标

**工作原理**：
- 首次使用时自动检测系统主题偏好（浅色或深色）
- 手动切换后，偏好会保存到浏览器本地存储
- 下次打开页面时自动应用上次选择的主题
- 代码高亮样式会同步切换（亮色使用 github 主题，暗色使用 github-dark 主题）

**深色模式特性**：
- 完整的深色配色方案，覆盖所有界面元素
- 编辑区和预览区均适配深色
- 代码块自动使用深色高亮主题
- 减少夜间使用时的视觉疲劳

### 智能续行

编辑器支持在按 Enter 键时自动续行，提升列表和引用的编辑效率：

**无序列表续行**：
- 当前行以 `- `、`* ` 或 `+ ` 开头时，自动在新行插入相同标记
- 示例：输入 `- 项目一` 后按 Enter，自动生成 `- `

**有序列表续行**：
- 当前行以 `1. ` 等数字标记开头时，自动在新行递增序号
- 示例：输入 `1. 第一项` 后按 Enter，自动生成 `2. `

**Checkbox 列表续行**：
- 当前行为 Checkbox 项（`- [ ] ` 或 `- [x] `）时，自动在新行插入 `- [ ] `
- 示例：输入 `- [x] 已完成` 后按 Enter，自动生成 `- [ ] `

**引用续行**：
- 当前行以 `> ` 开头时，自动在新行插入 `> `
- 示例：输入 `> 引用内容` 后按 Enter，自动生成 `> `

**空标记自动清除**：
- 在空列表项（如只有 `- `）或空引用行（如只有 `> `）上按 Enter，会自动删除该标记符并换行
- 无需手动退格删除，快速退出列表/引用模式

### GFM 与 Breaks 模式

**GFM（GitHub Flavored Markdown）**：
- 编辑器默认启用 GFM 模式
- 支持删除线（`~~文本~~`）、任务列表（`- [x]`）、表格等扩展语法
- 兼容 GitHub 上的 Markdown 渲染效果

**Breaks 模式**：
- 默认启用 Breaks 模式
- 单个换行符即可生成 `<br>` 换行，无需空行
- 更符合中文写作习惯

---

## 🛡️ 安全特性

### XSS 防护

编辑器使用 DOMPurify 对预览内容进行严格过滤，防止 XSS 攻击：
- 仅允许安全的 HTML 标签通过（如 `h1-h6`、`p`、`a`、`img`、`code`、`pre`、`table` 等）
- 禁止 `data-*` 等自定义属性
- 过滤所有潜在危险的脚本和事件处理属性

### 链接安全处理

- 所有外部链接自动添加 `target="_blank"` 和 `rel="noopener noreferrer"` 属性
- 阻止 `javascript:` 和 `data:` 等危险协议链接
- 确保点击链接时不会执行恶意代码

### 内容安全策略（CSP）

页面配置了 Content-Security-Policy，限制资源加载来源：
- 脚本和样式仅允许从自身和 jsdelivr CDN 加载
- 图片允许从自身、data URI 和 HTTPS 来源加载
- 进一步降低注入风险

### 本地数据安全

- 所有操作在本地浏览器中完成，不上传任何数据到服务器
- 草稿仅存储在浏览器 localStorage 中，其他网站无法访问

---

## ❓ 常见问题

### Q1: 预览区不更新怎么办？
**A**: 
1. 检查浏览器控制台是否有错误（按 F12）
2. 刷新页面重试
3. 确认浏览器兼容性

### Q2: 无法保存文件怎么办？
**A**:
1. 检查浏览器是否禁用了下载
2. 检查下载文件夹是否有写入权限
3. 尝试使用其他浏览器

### Q3: 草稿丢失了怎么办？
**A**:
1. 草稿存储在浏览器本地，清除缓存会丢失
2. 草稿保存期限为 7 天
3. 建议及时保存重要文档到本地

### Q4: 支持哪些浏览器？
**A**:
- ✅ Chrome（推荐）
- ✅ Firefox
- ✅ Edge
- ✅ Safari
- ❌ IE（不支持）

### Q5: 如何导出为 PDF？
**A**:
当前版本不支持直接导出 PDF，可以：
1. 使用浏览器打印功能（仅预览模式）
2. 复制预览内容到 Word 再转 PDF
3. 使用第三方 Markdown 转 PDF 工具

### Q6: 能否在移动设备上使用？
**A**:
可以，但体验可能不佳：
- 快捷键可能不可用
- 小屏幕显示受限
- 建议使用平板或电脑

### Q7: 数据安全吗？
**A**:
完全安全：
- 所有操作在本地浏览器完成
- 不上传任何数据到服务器
- 草稿仅存储在您的浏览器中

### Q8: 如何自定义样式？
**A**:
修改 `css/main.css` 文件，推荐通过 CSS 变量进行定制：
- 主题色：`--color-primary`、`--color-primary-hover`
- 编辑区样式：`.editor-textarea`
- 预览区样式：`.preview-content`
- 工具栏样式：`.toolbar`
- 深色模式变量：`[data-theme="dark"]` 下的对应变量
- 详细配置参见下方"自定义与配置"章节

### Q9: 为什么预览有延迟？
**A**:
- 使用了防抖机制（300ms）
- 避免频繁渲染影响性能
- 内容未变化时自动跳过渲染
- 防抖延迟可在 `app.js` 中调整

### Q10: 能否离线使用？
**A**:
部分可以：
- HTML、CSS、JS 可离线
- 第三方库使用 CDN，需要网络
- 建议下载库文件到本地实现完全离线

### Q11: 深色模式如何工作？
**A**:
- 首次访问自动检测系统主题偏好
- 手动切换后保存到浏览器，下次自动应用
- 代码高亮样式随主题同步切换
- 可随时通过工具栏按钮切换

### Q12: 支持哪些 Markdown 扩展语法？
**A**:
- 启用了 GFM 模式，支持删除线、任务列表、表格等
- 启用了 Breaks 模式，单个换行即可换行显示
- 具体支持范围取决于 marked.js 的 GFM 解析能力

---

## ⚙️ 自定义与配置

### CSS 变量定制

编辑器使用 CSS 变量（设计令牌）管理样式，修改 `css/main.css` 顶部的变量即可全局调整外观：

**颜色变量**（浅色模式）：
| 变量 | 默认值 | 用途 |
|------|--------|------|
| `--color-primary` | `#4f6ef7` | 主题色 |
| `--color-primary-hover` | `#3b5de7` | 主题色悬停态 |
| `--color-bg-app` | `#f0f2f5` | 应用背景色 |
| `--color-bg-editor` | `#fafbfc` | 编辑区背景色 |
| `--color-bg-preview` | `#ffffff` | 预览区背景色 |
| `--color-text-primary` | `#1a1a2e` | 主要文本色 |
| `--color-text-secondary` | `#555770` | 次要文本色 |
| `--color-border` | `#e2e4e9` | 边框色 |

**深色模式**：
在 `[data-theme="dark"]` 选择器下覆盖对应的颜色变量即可自定义深色主题配色。

**间距与圆角**：
| 变量 | 默认值 | 用途 |
|------|--------|------|
| `--radius-sm` | `5px` | 小圆角 |
| `--radius-md` | `8px` | 中圆角 |
| `--radius-lg` | `12px` | 大圆角 |

**字体**：
| 变量 | 默认值 | 用途 |
|------|--------|------|
| `--font-sans` | 系统字体栈 | 界面字体 |
| `--font-mono` | 等宽字体栈 | 编辑器和代码字体 |

### JavaScript 运行时配置

以下参数可在对应 JS 文件中调整：

| 配置项 | 默认值 | 文件 | 说明 |
|--------|--------|------|------|
| 预览防抖延迟 | `300ms` | `app.js` | 编辑后触发预览渲染的延迟 |
| 草稿保存延迟 | `2000ms` | `app.js` | 停止编辑后保存草稿的延迟 |
| 草稿过期天数 | `7` 天 | `file-manager.js` | 草稿自动过期时间 |
| 文件大小上限 | `10MB` | `utils.js` | 打开文件的最大体积 |
| Tab 缩进空格数 | `4` | `editor.js` | 按 Tab 键插入的空格数量 |

### 第三方依赖

| 库 | 版本 | 用途 |
|-----|------|------|
| marked.js | 11.1.1 | Markdown 解析渲染 |
| highlight.js | 11.9.0 | 代码语法高亮 |
| DOMPurify | 3.0.8 | HTML 内容安全过滤 |

**代码高亮已加载的语言**：JavaScript、Python、Java、HTML、CSS、JSON

**离线部署**：将上述 CDN 资源下载到本地，并修改 `index.html` 中的引用路径即可实现完全离线使用。

### 响应式设计

编辑器支持响应式布局：
- **768px 以上**：标准分屏布局
- **768px 以下**：编辑区和预览区纵向排列，工具栏按钮隐藏文字标签仅显示图标，适合平板使用

---

## 💡 最佳实践

### 1. 定期保存
- 使用 `Ctrl+S` 定期保存
- 重要文档建议使用版本控制（如 Git）

### 2. 使用快捷键
- 提高效率
- 减少鼠标操作
- 记住常用快捷键

### 3. 合理组织内容
- 使用标题层级结构
- 适当使用列表和表格
- 添加目录索引

### 4. 代码块标注语言
- 启用语法高亮
- 提高可读性
- 方便他人理解

### 5. 备份重要文档
- 草稿会过期
- 浏览器数据可能丢失
- 建议保存到云盘或 Git

---

## 🎯 使用技巧

### 快速插入当前日期
手动输入或使用快捷键

### 批量转换列表
1. 选中多行文本
2. 点击列表按钮
3. 每行自动转换为列表项

### 快速格式化选中文本
1. 选中文本
2. 点击工具栏按钮
3. 自动包裹语法

### 使用表格模板
1. 点击表格按钮
2. 获得基础模板
3. 根据需要修改行列

---

## 📞 获取帮助

如有问题或建议：
1. 查看 README.md 文档
2. 查看浏览器控制台错误信息
3. 检查是否为浏览器兼容性问题

---

**祝您使用愉快！** 🎉
